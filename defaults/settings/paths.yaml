append:
  permissions:
    paths:
      deny:
        - path: "~/.ssh/**"
          permissions: [read, edit, write, glob]
        - path: "~/.gnupg/**"
          permissions: [read, edit, write, glob]
        - path: "~/.var/app/**"
          permissions: [read, edit, write, glob]
        - path: "~/.mozilla/**"
          permissions: [read, edit, write, glob]
        - path: "~/.thunderbird/**"
          permissions: [read, edit, write, glob]
